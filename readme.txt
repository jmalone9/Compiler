This took longer than expected and will not produce any output yet, still a work in progress.
The plan is to finish up the tree generation (currently), then work on combining that with the symbol table to make the code generation happen, which we planned out a bit.
